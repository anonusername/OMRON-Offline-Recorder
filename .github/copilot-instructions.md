Project-specific instructions are in `.agent-files/OMRON-Offline-Recorder/`.
Only use files in `.agent-files/OMRON-Offline-Recorder/` for this repository.
If that folder is missing or empty, stay read-only and limit work to discovery and setup guidance.
Use `omron-venv` for all Python work; create it with Python 3.13 if missing, and never commit it.
Do not perform broad refactors, dependency changes, or large file edits until setup is complete.